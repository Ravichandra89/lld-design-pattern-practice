/**
 * Storing the proper user_details as an interface
 */

#ifndef USER_MODEL_HPP
#define USER_MODEL_HPP

#include <string>
using namespace std;

// define the user_data from structure
struct User {
    string name;
    string address;
};

#endif